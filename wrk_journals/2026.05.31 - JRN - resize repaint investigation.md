2026-05-31 18:50 - Started investigating Arthur's resize repaint bug. Screenshot shows stale/uncleared pixels after enlarging the main window, which feels more like an invalidation/background erase issue than nonogram-specific drawing logic, but I need to trace the handlers before blaming mdtpw.

Emotional temperature: focused, slightly wary of concurrent edits in this dirty tree. I need to avoid trampling Arthur's in-flight files and keep any writes inside the workspace.

2026-05-31 18:56 - Traced the Nonogram frame/display handlers and read the mdtpw OWL runtime read-only. The display child registers class style 3 (`CS_VREDRAW | CS_HREDRAW`), but the frame class `Nonogram Window` registers style 0. That makes the main frame dependent on minimal exposed-region invalidation during resize; it is a plausible match for Arthur's screenshot where the newly exposed frame/client area is stale.

Reproduction note: PrintWindow captures after programmatic resize are clean, but desktop `mdscreensnap` is black in this environment, so I cannot directly verify the visible DWM/desktop artifact here. Gut feel: this is still a real resize invalidation gap, and a narrow frame-class redraw flag is safer than touching the mdtpw runtime outside this workspace.

2026-05-31 18:49 - Applied the local fix in `NONOGRAM.PAS`: `TMainWindow.GetWindowClass` now ORs in `CS_HREDRAW | CS_VREDRAW`, matching the display child and making the frame invalidate/redraw on horizontal or vertical resize. Rebuilt with `mdtpw`; the executable launches and the registered frame class style is now 3.

Emotional temperature: cautiously satisfied. I still do not love that mdtpw's base `TWindow.GetWindowClass` defaults to style 0; that may be the broader framework issue Arthur suspected. I avoided editing `C:\language\mdtpw` because this task's instructions say not to write outside the workspace, and that repo is also dirty.

Timestamp correction: the earlier `18:56` note was my manual typo; it was written before the final rebuild/checks, not after them.

2026-05-31 19:30 - Arthur correctly pushed back that the app worked on Windows 3.1, so the real fix belongs in mdtpw. With permission to edit `C:\language\mdtpw`, I added a runtime regression there and moved the fix into `runtime_pas/WObjects.pas` (`TWindow.GetWindowClass` now defaults `cs_HRedraw or cs_VRedraw`). Removed the temporary Nonogram-side style override and rebuilt this app with the fixed mdtpw debug binary. The frame still registers style 3, now via inherited runtime behavior.

2026-05-31 19:59 - Follow-up fixes landed in mdtpw for the startup caption, file date, and EDITER `DIALOG_1` symptoms. Regenerated `generated/nonogram.rc` from the fixed resource converter and rebuilt `nonogram.exe`. The `EDITER` resource now has no `CAPTION` line, the rebuilt EXE no longer contains `DIALOG_1`, and screenshots in `shots\startup_after_fixes_287441806.png` / `shots\2026.05.31_195320_startup_desktop_after_fixes.png` show the main title bar painted at startup.

Emotional temperature: relieved. This one was a neat reminder that compatibility bugs often stack: a non-client paint width bug, a DOS date packing bug, and a resource-style interpretation bug all surfaced as "the old app looks wrong."
