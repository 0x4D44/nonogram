# 2026-05-31 - Uncommitted change audit

## Initial audit
- Arthur asked to inspect the dirty tree, decide what should be committed or ignored, group commits sensibly, include `wrk_docs` and `wrk_journals`, then push.
- The repo has no local `AGENTS.md`; continuing from Arthur's prompt instructions.
- Dirty changes cover:
  - Native Win32/MDI app fixes in `NONOGRAM.PAS`.
  - Regenerated/patched Win32 resources in `generated/nonogram.rc`.
  - Build output retargeting in `mdtpw.toml` from `nonogram_mdtpw.exe` to `nonogram.exe`.
  - A rebuilt native `NONOGRAM.EXE`.
  - Preserved original Win16 executable under `WIN16/NONOGRAM.EXE`.
  - File-open automation compatibility in `solve-puzzle.ps1`.
  - `snowman.ngm` delimiter normalization.
  - Journal updates/additions for the prior work.
  - `.mdtpw-imports/`, which is generated import-library cache material and should be ignored.

## Grouping decision
- Commit `.gitignore` and this audit journal first to record the cache ignore decision.
- Commit the native Win32 GUI/build/resource work together because the Pascal source, regenerated resources, output path, built exe, preserved Win16 exe, smoke script, and journals describe one porting slice.
- Commit `snowman.ngm` separately because it is data-format cleanup unrelated to the executable port.

## Emotional temperature
- Careful and slightly suspicious of historical binary handling. The `WIN16/NONOGRAM.EXE` blob hash matches `HEAD:NONOGRAM.EXE`, so tracking it is a defensible preservation move before the root executable becomes the native build artifact.

## Validation
- `mdtimeout 180s -- mdtpw` completed successfully.
- Build output: `nonogram.exe` PE32 Windows, about 243 KB.
- Static `.NGM` sweep: all 58 root + `_puzzles` files pass clue-total validation.
- GUI smoke: `solve-puzzle.ps1` loaded and solved `MARYMARY.NGM` with `nonogram.exe`; screenshot saved as `C:\language\nonogram\shots\audit-marymary_solved.png` and status bar showed `Successfully solved`.
- `.mdtpw-imports/` disappeared from `git status` after adding the ignore rule, confirming it is treated as local generated cache.
